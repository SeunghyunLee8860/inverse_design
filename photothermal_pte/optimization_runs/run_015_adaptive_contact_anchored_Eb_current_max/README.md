# Run 015 — adaptive E||b current maximization

This is the independent `E||b` counterpart of Run014 and starts from its own
uniform `rho=0.5` density.  It uses exactly the same adaptive continuation,
feature audit, geometry, and physics contract; only incident polarization is
changed (`Lumerical x=b`, polarization angle 0 deg).

Run015 starts automatically only after Run014 records continuous completion.
