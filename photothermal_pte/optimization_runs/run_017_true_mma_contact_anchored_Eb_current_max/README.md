# Run 017 — fresh true-MMA E||b optimization

This is the independent `E||b` counterpart to Run016. It starts from its own
exact uniform `rho=0.5` field and uses the identical geometry, physics,
constraints, MMA logic and convergence gates. Only the incident polarization
changes to Lumerical angle 0 degrees (`x=b`).

Run017 starts only after Run016 has a fresh exact-binary GPU-Maxwell,
CUDA-thermal and electrical objective certificate.
