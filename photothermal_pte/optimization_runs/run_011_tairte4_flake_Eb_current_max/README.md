# Run 011 — TaIrTe4-flake current maximization for E parallel b

This run will start independently from the same uniform physical density and
validated TaIrTe4-to-void flake contract as Run 010.  Lumerical coordinates
are `x=b`, `y=a`; the source polarization angle is 0 degrees.  It does not
inherit the E-parallel-a optimized density.

Status: setup and coupled-AD validation in progress; optimization has not
started.

