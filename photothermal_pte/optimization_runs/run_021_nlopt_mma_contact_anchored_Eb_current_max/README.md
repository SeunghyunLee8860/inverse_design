# Run021 — production NLopt LD_MMA, E parallel b

This fresh exact-uniform run starts automatically only after Run020 `E||a`
completes its exact-binary validation.  It uses the identical geometry,
constraints, continuation and NLopt stopping contract, changing only the
source polarization to `E||b`.
